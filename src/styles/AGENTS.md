# Стили

- В `_variables.scss` каждую тематическую переменную задавай как `$name: var(--name, <значение>);`. Имя без `$` совпадает с CSS-свойством. Переопределение — `--name` в `:root`.
- В компонентах подключай `@use "@/styles/kit" as *;`, цвета бери из `$rd-*`.
